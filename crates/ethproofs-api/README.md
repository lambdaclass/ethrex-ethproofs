# EthProofs JSON RPC API Wrapper in Rust

This crate provides a Rust client for interacting with the EthProofs JSON RPC API. It allows users to easily make requests to the EthProofs service and handle responses in a type-safe manner.
